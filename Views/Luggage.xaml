<UserControl x:Class="UserModule.Views.Luggage"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             
             xmlns:comp="clr-namespace:UserModule.Components"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="600" MinHeight="600" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <!-- Style for rounded TextBox with visible border and watermark -->
        <Style x:Key="RoundedWatermarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                CornerRadius="4"
                                Background="{TemplateBinding Background}"  
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"/>
                                <TextBlock x:Name="Watermark"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#333"
                                           Margin="14,0,0,0"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Opacity="0.6"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsFocused" Value="False"/>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for ComboBox with white background -->
        <Style x:Key="WhiteComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Background="White"
                                          BorderBrush="#999999"
                                          BorderThickness="1"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="White" 
                                                            BorderBrush="#999999" 
                                                            BorderThickness="1"
                                                            CornerRadius="4">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition/>
                                                                <ColumnDefinition Width="30"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Path x:Name="Arrow" Grid.Column="1"
                                                                  Fill="#666" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                Placement="Bottom"
                                PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="White" 
                                            BorderThickness="1" BorderBrush="#999999"
                                            CornerRadius="4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"
                                                    Background="White"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for ComboBox Items with white background -->
        <Style x:Key="WhiteComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E6F0FE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for DataGrid header to match Dashboard -->
        <Style x:Key="LuggageDataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F0F3FF"/>
            <Setter Property="Foreground" Value="#4F5E80"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar Style - Completely hidden but functional -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="0"/>
            <Setter Property="Height" Value="0"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <!-- Empty template to make scrollbar invisible -->
                        <Border Background="Transparent" Width="0" Height="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Content Area - FIXED AT TOP -->
        <Grid Grid.Row="0" Margin="0,10,0,0" VerticalAlignment="Top">
            <Border Background="#232323" CornerRadius="8" Padding="20" Margin="24,10,20,10" VerticalAlignment="Top">
                <Grid>
                    <!-- Two columns: left for title, right for Booking ID -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Left side: Title and subtitle -->
                    <StackPanel Orientation="Vertical" Grid.Column="0">
                        <TextBlock Text="Add New Booking"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="White"/>
                        <TextBlock Text="Fill in the details to create a new booking entry"
                       FontSize="13"
                       Foreground="#F7F7F7"
                       Margin="0,6,0,0"/>
                    </StackPanel>

                    <!-- Right side: Booking ID -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Booking ID"
                             FontWeight="SemiBold"
                             FontSize="14"
                             Foreground="White"
                             VerticalAlignment="Center"
                             Margin="0,0,10,0"
                             Visibility="Collapsed"
                        />
                        <TextBox x:Name="txtBookingID"
                            Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                            Tag="Booking ID"
                            Foreground="#333333"
                            Background="White"
                            Height="40"
                            Width="180"
                            IsReadOnly="True"
                            BorderThickness="0"
                            Padding="10,0"
                            Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
        
        <!-- FORM - SCROLLABLE AREA -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="30" Margin="20,10,20,20">
            <ScrollViewer x:Name="FormScrollViewer"
                          VerticalScrollBarVisibility="Hidden" 
                          HorizontalScrollBarVisibility="Disabled"
                          PanningMode="VerticalOnly"
                          CanContentScroll="False"
                          PreviewMouseWheel="UserControl_PreviewMouseWheel">
                <!-- Main content grid (your booking form) -->
                <Grid x:Name="MainContentGrid" Margin="12,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Customer Info"
                     FontWeight="SemiBold"
                     FontSize="16"
                     Foreground="#333"
                     Grid.Row="0"
                     Grid.Column="0"
                     Grid.ColumnSpan="3"
                     HorizontalAlignment="Left"
                     Margin="0,0,0,12"/>
                    
                    <!-- First Name -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,30,15,20">
                        <TextBlock Text="First Name"
                       FontWeight="SemiBold"
                       FontSize="14"
                       Foreground="#333"
                       Margin="0,0,0,8"/>
                        <TextBox x:Name="txtFirstName"
                     Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                     Tag="Enter first name"
                     Height="40"
                     Foreground="Black"
                     FontWeight="DemiBold"
                     PreviewKeyDown="FirstName_PreviewKeyDown"
                     PreviewTextInput="UppercaseOnly_PreviewTextInput"
                     DataObject.Pasting="UppercaseOnly_Pasting"
                     TextChanged="CustomerName_TextChanged"/>
                        <TextBlock x:Name="errCustomer1"
                       Text="* Fill the first name"
                       Foreground="#CC1F1F"
                       FontSize="12"
                       Visibility="Collapsed"
                       Margin="5,2,0,0"/>
                    </StackPanel>

                    <!-- Last Name -->
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,30,15,20">
                        <TextBlock Text="Last Name"
                       FontWeight="SemiBold"
                       FontSize="14"
                       Foreground="#333"
                       Margin="0,0,0,8"/>
                        <TextBox x:Name="txtLastName"
                     Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                     Tag="Enter last name"
                     Height="40"
                     Foreground="Black"
                     FontWeight="DemiBold"
                     PreviewKeyDown="LastName_PreviewKeyDown"
                     PreviewTextInput="UppercaseOnly_PreviewTextInput"
                     DataObject.Pasting="UppercaseOnly_Pasting"
                     TextChanged="CustomerName_TextChanged"/>
                    </StackPanel>

                    <!-- Phone Number -->
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="15,30,0,20" Background="Transparent">
                        <TextBlock Text="Phone Number" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtPhone" 
                     Style="{StaticResource RoundedWatermarkTextBoxStyle}" 
                     Tag="+91 902 543 xxxx" 
                     Foreground="Black" 
                     FontWeight="DemiBold" 
                     Height="40"  
                     MaxLength="10" 
                     PreviewKeyDown="Phone_PreviewKeyDown" 
                     PreviewTextInput="NumbersOnly_PreviewTextInput" 
                     DataObject.Pasting="NumbersOnly_Pasting"/>
                        <TextBlock x:Name="errPhone1" Text="* Fill Phone Number" Foreground="#CC1F1F" FontSize="12" Visibility="Collapsed" Margin="5,2,0,0"/>
                    </StackPanel>

                    <!-- Hidden combined field -->
                    <TextBox x:Name="txtCustomer" Visibility="Collapsed"/>

                    <!-- Proof Information - 1 Row, 4 Columns -->
                    <Grid Grid.Row="4" Grid.ColumnSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Section Title -->
                        <TextBlock Text="Proof Information" 
                                   FontWeight="SemiBold" 
                                   FontSize="14" 
                                   Foreground="#222" 
                                   Grid.Row="0"
                                   Margin="0,0,0,14"/>

                        <!-- 1 Row, 4 Columns Grid for ID Type, ID Number, Date, Time -->
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Column 0: ID Type -->
                            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                <TextBlock Text="ID Type" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                                <ComboBox x:Name="cmbIdType"
                                          Height="40"
                                          Padding="10,0"
                                          FontSize="14"
                                          Foreground="#333"
                                          Background="White"
                                          HorizontalContentAlignment="Left"
                                          VerticalContentAlignment="Center"
                                          MaxDropDownHeight="150"
                                          PreviewKeyDown="IdType_PreviewKeyDown"
                                          Style="{StaticResource WhiteComboBoxStyle}"
                                          SelectionChanged="cmbIdType_SelectionChanged">
                                    <ComboBoxItem x:Name="idPlaceholder" Content="Select ID" IsEnabled="False" IsSelected="True"/>
                                    <ComboBoxItem Content="Aadhar" Style="{StaticResource WhiteComboBoxItemStyle}"/>
                                    <ComboBoxItem Content="PNR Number" Style="{StaticResource WhiteComboBoxItemStyle}"/>
                                    <ComboBoxItem Content="PAN ID" Style="{StaticResource WhiteComboBoxItemStyle}"/>
                                </ComboBox>
                                <TextBlock x:Name="errIdType1" Text="* Select ID Type" Foreground="#CC1F1F" FontSize="12" Visibility="Collapsed" Margin="5,2,0,0"/>
                            </StackPanel>

                            <!-- Column 1: ID Number -->
                            <StackPanel Grid.Column="1" Margin="15,0,15,0">
                                <TextBlock x:Name="lblIdInput" Text="Enter ID Number" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                                <TextBox x:Name="txtIdNumber"
                                         Foreground="Black" 
                                         FontWeight="DemiBold"
                                         Style="{StaticResource RoundedWatermarkTextBoxStyle}" 
                                         Tag="ID Number" 
                                         Height="40"
                                         TextWrapping="NoWrap"
                                         AcceptsReturn="False"
                                         PreviewKeyDown="IdNumber_PreviewKeyDown"
                                         PreviewTextInput="IdNumber_PreviewTextInput"
                                         DataObject.Pasting="IdNumber_Pasting"/>
                                <TextBlock x:Name="errIdNumber1" Text="* Fill ID Number" Foreground="#CC1F1F" FontSize="12" Visibility="Collapsed" Margin="5,2,0,0"/>
                            </StackPanel>

                            <!-- Column 2: Date -->
                            <StackPanel Grid.Column="2" Margin="15,0,15,0">
                                <TextBlock Text="Date" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                                <TextBox x:Name="txtBookingDate"
                                     Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                                     Foreground="Black" 
                                     FontWeight="DemiBold"
                                     Height="40"
                                     IsReadOnly="True"/>
                            </StackPanel>

                            <!-- Column 3: Time -->
                            <StackPanel Grid.Column="3" Margin="15,0,0,0">
                                <TextBlock Text="Time" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                                <TextBox x:Name="txtBookingTime"
                                     Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                                     Foreground="Black" 
                                     FontWeight="DemiBold"
                                     Tag="HH:MM"
                                     Height="40"
                                     IsReadOnly="True"/>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <!-- Luggage Details Section -->
                    <Grid Grid.Row="5" Grid.ColumnSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Section Title -->
                        <TextBlock Text="Luggage Details" 
                                   FontWeight="SemiBold" 
                                   FontSize="14" 
                                   Foreground="#222" 
                                   Grid.Row="0"
                                   Margin="0,0,0,14"/>

                        <!-- 1 Row, 2 Columns Grid for Locker Count and Locker Number -->
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Column 0: Locker Count -->
                            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                <TextBlock Text="Locker Count" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                                <TextBox x:Name="txtLockerCount"
                                     Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                                     Tag="Enter locker count "
                                     Foreground="Black" 
                                     FontWeight="DemiBold"
                                     Height="40"
                                     Text=""
                                     MaxLength="2"
                                     PreviewTextInput="NumbersOnly_PreviewTextInput"
                                     DataObject.Pasting="NumbersOnly_Pasting"
                                     PreviewKeyDown="LockerCount_PreviewKeyDown"
                                     TextChanged="LockerCount_TextChanged"/>
                                <TextBlock x:Name="errLockerCount" 
                                       Text="* Locker count must be between 1 and 60" 
                                       Foreground="#CC1F1F" 
                                       FontSize="12" 
                                       Visibility="Collapsed" 
                                       Margin="5,2,0,0"/>
                            </StackPanel>

                            <!-- Column 1: Locker Number (Multiple Selection) -->
                            <StackPanel Grid.Column="1" Margin="15,0,0,0">
                                <TextBlock Text="Locker Number" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                                <Grid>
                                    <!-- TextBox with dropdown arrow button -->
                                    <Border BorderBrush="#999999"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Background="White"
                                            Height="40"
                                            MouseEnter="LockerNumberBorder_MouseEnter">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="30"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- TextBox for displaying selected lockers with count -->
                                            <Grid Grid.Column="0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBox x:Name="txtLockerNumbers"
                                                         Grid.Column="0"
                                                         BorderThickness="0"
                                                         Background="Transparent"
                                                         Padding="12,0,5,0"
                                                         FontSize="16"
                                                         VerticalContentAlignment="Center"
                                                         Foreground="Black"
                                                         FontWeight="DemiBold"
                                                         IsReadOnly="True"
                                                         PreviewKeyDown="LockerNumber_PreviewKeyDown"
                                                         PreviewMouseDown="LockerNumbers_MouseDown"
                                                         Cursor="Hand"
                                                         ToolTip="Hover or click to select lockers">
                                                <TextBox.Style>
                                                    <Style TargetType="TextBox">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="TextBox">
                                                                    <Grid>
                                                                        <ScrollViewer x:Name="PART_ContentHost"
                                                                                      VerticalAlignment="Center"/>
                                                                        <TextBlock x:Name="Watermark"
                                                                                   Text="Select locker numbers"
                                                                                   Foreground="#999"
                                                                                   Margin="12,0,0,0"
                                                                                   VerticalAlignment="Center"
                                                                                   IsHitTestVisible="False"
                                                                                   Opacity="0.6"
                                                                                   Visibility="Collapsed"/>
                                                                    </Grid>
                                                                    <ControlTemplate.Triggers>
                                                                        <MultiTrigger>
                                                                            <MultiTrigger.Conditions>
                                                                                <Condition Property="Text" Value=""/>
                                                                            </MultiTrigger.Conditions>
                                                                            <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                                                                        </MultiTrigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </TextBox.Style>
                                            </TextBox>
                                            <!-- Count indicator -->
                                            <TextBlock x:Name="txtLockerCount_Display"
                                                       Grid.Column="1"
                                                       Foreground="#666"
                                                       FontSize="13"
                                                       FontWeight="Normal"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"
                                                       Visibility="Collapsed"/>
                                        </Grid>
                                            
                                            <!-- Dropdown arrow button -->
                                            <Button Grid.Column="1"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Cursor="Hand"
                                                    ToolTip="Open locker selection"
                                                    Click="LockerNumberDropdown_Click"
                                                    Focusable="False">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            Padding="{TemplateBinding Padding}">
                                                                        <Path x:Name="Arrow"
                                                                              Fill="#666"
                                                                              HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"
                                                                              Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter TargetName="Arrow" Property="Fill" Value="#333"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsPressed" Value="True">
                                                                            <Setter TargetName="Arrow" Property="Fill" Value="#000"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </Grid>
                                    </Border>
                                    
                                    <!-- Popup for locker selection -->
                                    <Popup x:Name="lockerNumberPopup"
                                           PlacementTarget="{Binding ElementName=txtLockerNumbers}"
                                           Placement="Bottom"
                                           StaysOpen="False"
                                           Width="400"
                                           Opened="LockerNumberPopup_Opened">
                                        <Border Background="White"
                                                BorderBrush="#999999"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                MaxHeight="300"
                                                MinHeight="150">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>
                                                
                                                <!-- Search box at top -->
                                                <Border Grid.Row="0"
                                                        Background="#F8F9FA"
                                                        BorderBrush="#E0E0E0"
                                                        BorderThickness="0,0,0,1"
                                                        Padding="10,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBox x:Name="txtLockerSearch"
                                                                 Grid.Column="0"
                                                                 Height="32"
                                                                 Padding="8,0"
                                                                 FontSize="14"
                                                                 Foreground="#333"
                                                                 Background="White"
                                                                 BorderBrush="#CCC"
                                                                 BorderThickness="1"
                                                                 VerticalContentAlignment="Center"
                                                                 TextChanged="LockerSearch_TextChanged"
                                                                 PreviewKeyDown="LockerSearch_PreviewKeyDown">
                                                        <TextBox.Style>
                                                            <Style TargetType="TextBox">
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="TextBox">
                                                                            <Border Background="{TemplateBinding Background}"
                                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                                                    CornerRadius="3">
                                                                                <Grid>
                                                                                    <ScrollViewer x:Name="PART_ContentHost"
                                                                                                  Margin="8,0"
                                                                                                  VerticalAlignment="Center"/>
                                                                                    <TextBlock x:Name="Watermark"
                                                                                               Text="Search locker..."
                                                                                               Foreground="#999"
                                                                                               Margin="8,0"
                                                                                               VerticalAlignment="Center"
                                                                                               IsHitTestVisible="False"
                                                                                               Visibility="Collapsed"/>
                                                                                </Grid>
                                                                            </Border>
                                                                            <ControlTemplate.Triggers>
                                                                                <MultiTrigger>
                                                                                    <MultiTrigger.Conditions>
                                                                                        <Condition Property="Text" Value=""/>
                                                                                    </MultiTrigger.Conditions>
                                                                                    <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                                                                                </MultiTrigger>
                                                                            </ControlTemplate.Triggers>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Style>
                                                        </TextBox.Style>
                                                    </TextBox>
                                                    <Button Grid.Column="1"
                                                            Content="Add"
                                                            Margin="8,0,0,0"
                                                            Padding="16,6"
                                                            Height="32"
                                                            FontSize="13"
                                                            FontWeight="SemiBold"
                                                            Foreground="White"
                                                            Background="#4CAF50"
                                                            BorderThickness="0"
                                                            Cursor="Hand"
                                                            Click="AddLockerNumber_Click">
                                                        <Button.Style>
                                                            <Style TargetType="Button">
                                                                <Setter Property="Template">
                                                                    <Setter.Value>
                                                                        <ControlTemplate TargetType="Button">
                                                                            <Border Background="{TemplateBinding Background}"
                                                                                    CornerRadius="3"
                                                                                    Padding="{TemplateBinding Padding}">
                                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                                  VerticalAlignment="Center"/>
                                                                            </Border>
                                                                            <ControlTemplate.Triggers>
                                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                                    <Setter Property="Background" Value="#45A049"/>
                                                                                </Trigger>
                                                                                <Trigger Property="IsPressed" Value="True">
                                                                                    <Setter Property="Background" Value="#3D8B40"/>
                                                                                </Trigger>
                                                                            </ControlTemplate.Triggers>
                                                                        </ControlTemplate>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </Style>
                                                        </Button.Style>
                                                    </Button>
                                                </Grid>
                                                </Border>
                                                
                                                <!-- Locker list with virtualization -->
                                                <ScrollViewer Grid.Row="1"
                                                              VerticalScrollBarVisibility="Auto"
                                                              HorizontalScrollBarVisibility="Disabled">
                                                    <ListBox x:Name="lstLockerNumbers"
                                                             SelectionMode="Multiple"
                                                             Background="White"
                                                             BorderThickness="0"
                                                             Padding="4"
                                                             MinHeight="100"
                                                             VirtualizingPanel.IsVirtualizing="True"
                                                             VirtualizingPanel.VirtualizationMode="Recycling"
                                                             ScrollViewer.CanContentScroll="True"
                                                             SelectionChanged="LockerNumbers_SelectionChanged">
                                                        <ListBox.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <VirtualizingStackPanel/>
                                                            </ItemsPanelTemplate>
                                                        </ListBox.ItemsPanel>
                                                    <ListBox.ItemContainerStyle>
                                                        <Style TargetType="ListBoxItem">
                                                            <Setter Property="Padding" Value="10,8"/>
                                                            <Setter Property="Background" Value="White"/>
                                                            <Setter Property="FontSize" Value="14"/>
                                                            <Setter Property="FontWeight" Value="Normal"/>
                                                            <Setter Property="Foreground" Value="#333"/>
                                                            <Setter Property="Cursor" Value="Hand"/>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#F0F0F0"/>
                                                                </Trigger>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter Property="Background" Value="#E6F0FE"/>
                                                                    <Setter Property="Foreground" Value="#333"/>
                                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </ListBox.ItemContainerStyle>
                                                </ListBox>
                                            </ScrollViewer>
                                        </Grid>
                                    </Border>
                                </Popup>
                                </Grid>
                                <TextBlock x:Name="errLockerNumber" 
                                       Text="* Select at least one locker number" 
                                       Foreground="#CC1F1F" 
                                       FontSize="12" 
                                       Visibility="Collapsed" 
                                       Margin="5,2,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <!-- Data grid for luggage items -->
                    <Border Grid.Row="6" Grid.ColumnSpan="3" Margin="0,0,0,10" Background="#FFFFFF" CornerRadius="8" Padding="8">
                        <DataGrid x:Name="LuggageGrid"
                              ItemsSource="{Binding Items}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="False"
                              CanUserResizeColumns="True"
                              CanUserResizeRows="False"
                              CanUserSortColumns="False"
                              SelectionUnit="Cell"
                              SelectionMode="Single"
                              HeadersVisibility="Column"
                              IsReadOnly="False"
                              RowHeight="40"
                              ColumnHeaderHeight="40"
                              GridLinesVisibility="None"
                              BorderThickness="0"
                              Background="#FAFBFF"
                              Height="Auto"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              PreviewKeyDown="LuggageGrid_PreviewKeyDown"
                              PreviewMouseLeftButtonDown="LuggageGrid_PreviewMouseLeftButtonDown"
                              PreviewMouseWheel="LuggageGrid_PreviewMouseWheel"
                              EnableRowVirtualization="True"
                              EnableColumnVirtualization="False"
                              IsSynchronizedWithCurrentItem="False">

                        <DataGrid.Resources>
                            <!-- Cell Style -->
                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="IsTabStop" Value="True"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="DataGridCell">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Stretch"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                    <Trigger Property="IsEditing" Value="True">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </Trigger>
                                    <MultiTrigger>
                                        <MultiTrigger.Conditions>
                                            <Condition Property="IsSelected" Value="True"/>
                                            <Condition Property="IsEditing" Value="True"/>
                                        </MultiTrigger.Conditions>
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                    </MultiTrigger>
                                </Style.Triggers>
                            </Style>

                            <!-- Row Style -->
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="IsTabStop" Value="False"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <!-- Column Header Style -->
                            <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource LuggageDataGridHeaderStyle}"/>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <!-- S.No Column -->
                            <DataGridTemplateColumn Width="80">
                                <DataGridTemplateColumn.Header>
                                    <TextBlock Text="S.No"
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"/>
                                </DataGridTemplateColumn.Header>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SNo}"
                                           FontSize="14"
                                           FontWeight="DemiBold"
                                           Padding="10,8"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Background="Transparent"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Luggage Type -->
                            <DataGridTemplateColumn Header="Luggage Type" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="Transparent" Padding="8,4">
                                            <TextBlock Text="{Binding LuggageType}"
                                               FontSize="14"
                                               FontWeight="DemiBold"
                                               HorizontalAlignment="Left"
                                               VerticalAlignment="Center"
                                               Background="Transparent"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <ComboBox IsEditable="True"
                                          IsTextSearchEnabled="True"
                                          StaysOpenOnEdit="True"
                                          ItemsSource="{Binding DataContext.LuggageTypes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                          Text="{Binding LuggageType, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                          Background="White"
                                          FontSize="14"
                                          FontWeight="DemiBold"
                                          Foreground="Black"
                                          BorderThickness="1"
                                          BorderBrush="#CCCCCC"
                                          Margin="2"
                                          Padding="8,4"
                                          MaxDropDownHeight="200"
                                          IsDropDownOpen="False"
                                          LostFocus="LuggageType_LostFocus"
                                          KeyUp="LuggageType_KeyUp"
                                          DropDownOpened="LuggageType_DropDownOpened"
                                          GotFocus="LuggageType_GotFocus"
                                          PreviewKeyDown="LuggageType_PreviewKeyDown"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Quantity -->
                            <DataGridTemplateColumn Header="Quantity" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="Transparent" Padding="8,4">
                                            <TextBlock Text="{Binding Quantity}"
                                               FontSize="14"
                                               FontWeight="DemiBold"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Background="Transparent"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                         PreviewTextInput="NumbersOnly_PreviewTextInput"
                                         Background="White"
                                         Foreground="Black"
                                         FontSize="14"
                                         FontWeight="DemiBold"
                                         BorderThickness="1"
                                         BorderBrush="#CCCCCC"
                                         Margin="2"
                                         Padding="8,4"
                                         TextAlignment="Center"
                                         VerticalContentAlignment="Center"
                                         MaxLength="2"
                                         LostFocus="Quantity_LostFocus"
                                         PreviewKeyDown="Quantity_PreviewKeyDown"
                                         GotFocus="Quantity_GotFocus"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>

                            <!-- Price -->
                            <DataGridTextColumn Header="Price"
                                    Binding="{Binding Price, StringFormat=0}"
                                    IsReadOnly="True"
                                    Width="100">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="VerticalAlignment" Value="Center"/>
                                        <Setter Property="TextAlignment" Value="Center"/>
                                        <Setter Property="Padding" Value="8,4"/>
                                        <Setter Property="FontSize" Value="14"/>
                                        <Setter Property="FontWeight" Value="DemiBold"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Focusable" Value="False"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Total -->
                            <DataGridTemplateColumn Header="Total" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="Transparent" Padding="8,4">
                                            <TextBlock Text="{Binding TotalAmount, StringFormat=0}"
                                               FontSize="14"
                                               FontWeight="DemiBold"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Background="Transparent"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                                <DataGridTemplateColumn.CellEditingTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding TotalAmount, Mode=TwoWay, UpdateSourceTrigger=LostFocus, StringFormat=0}"
                                         PreviewTextInput="NumbersOnly_PreviewTextInput"
                                         DataObject.Pasting="NumbersOnly_Pasting"
                                         Background="White"
                                         Foreground="Black"
                                         FontSize="14"
                                         FontWeight="DemiBold"
                                         BorderThickness="1"
                                         BorderBrush="#CCCCCC"
                                         Margin="2"
                                         Padding="8,4"
                                         TextAlignment="Center"
                                         VerticalContentAlignment="Center"
                                         MaxLength="10"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellEditingTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Submit / Cancel Buttons -->
                <Grid Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="0,40,0,0">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                        <Button x:Name="GenerateBillButton" Content="Generate Bill" Width="249" Height="40" Background="#232323" Foreground="white" FontSize="16" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0" Click="GenerateBill_Click"/>
                        <Button Content="Cancel" Width="243" Height="40" Background="#BDBCBC" Foreground="White" FontSize="16" FontWeight="SemiBold" BorderBrush="#999" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
