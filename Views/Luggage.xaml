<UserControl x:Class="UserModule.Views.Luggage"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             
             xmlns:comp="clr-namespace:UserModule.Components"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="600" MinHeight="600" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <!-- Style for rounded TextBox with visible border and watermark -->
        <Style x:Key="RoundedWatermarkTextBoxStyle" TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd"
                                CornerRadius="4"
                                Background="{TemplateBinding Background}"  
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"/>
                                <TextBlock x:Name="Watermark"
                                           Text="{TemplateBinding Tag}"
                                           Foreground="#333"
                                           Margin="14,0,0,0"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Opacity="0.6"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsFocused" Value="False"/>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for ComboBox with white background -->
        <Style x:Key="WhiteComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#999999"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Background="White"
                                          BorderBrush="#999999"
                                          BorderThickness="1"
                                          Focusable="False"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="White" 
                                                            BorderBrush="#999999" 
                                                            BorderThickness="1"
                                                            CornerRadius="4">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition/>
                                                                <ColumnDefinition Width="30"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Path x:Name="Arrow" Grid.Column="1"
                                                                  Fill="#666" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                        </Grid>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                Placement="Bottom"
                                PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                IsOpen="{TemplateBinding IsDropDownOpen}"
                                AllowsTransparency="True"
                                Focusable="False"
                                PopupAnimation="Slide">

                                <Grid x:Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="White" 
                                            BorderThickness="1" BorderBrush="#999999"
                                            CornerRadius="4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"
                                                    Background="White"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for ComboBox Items with white background -->
        <Style x:Key="WhiteComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E6F0FE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for DataGrid header to match Dashboard -->
        <Style x:Key="LuggageDataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F0F3FF"/>
            <Setter Property="Foreground" Value="#4F5E80"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar Style - Completely hidden but functional -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="0"/>
            <Setter Property="Height" Value="0"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <!-- Empty template to make scrollbar invisible -->
                        <Border Background="Transparent" Width="0" Height="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Content Area -->
        <Grid Grid.Row="0" Background="#F7F7F7" Margin="0,10,0,20" VerticalAlignment="Top">
            <Border Background="#232323" CornerRadius="8" Padding="20" Margin="24,10,20,10" VerticalAlignment="Top">
                <Grid>
                    <!-- Two columns: left for title, right for Booking ID -->
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Left side: Title and subtitle -->
                    <StackPanel Orientation="Vertical" Grid.Column="0">
                        <TextBlock Text="Add New Booking"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="White"/>
                        <TextBlock Text="Fill in the details to create a new booking entry"
                       FontSize="13"
                       Foreground="#F7F7F7"
                       Margin="0,6,0,0"/>
                    </StackPanel>

                    <!-- Right side: Booking ID -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Booking ID"
                             FontWeight="SemiBold"
                             FontSize="14"
                             Foreground="White"
                             VerticalAlignment="Center"
                             Margin="0,0,10,0"
                             Visibility="Collapsed"
                        />
                        <TextBox x:Name="txtBookingID"
                            Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                            Tag="Booking ID"
                            Foreground="#333333"
                            Background="White"
                            Height="40"
                            Width="180"
                            IsReadOnly="True"
                            BorderThickness="0"
                            Padding="10,0"
                            Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>
        <!-- FORM -->
        <Border Background="White" CornerRadius="8" Padding="30" Margin="20,120,20,20">
            <Grid>
                <!-- Main content grid (your booking form) -->
                <Grid x:Name="MainContentGrid" Margin="12,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Customer Info"
                     FontWeight="SemiBold"
                     FontSize="16"
                     Foreground="#333"
                     Grid.Row="0"
                     Grid.Column="0"
                     Grid.ColumnSpan="3"
                     HorizontalAlignment="Left"
                     Margin="0,0,0,12"/>
                    <!-- First Name -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,30,15,20">
                        <TextBlock Text="First Name"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="#333"
                           Margin="0,0,0,8"/>
                        <TextBox x:Name="txtFirstName"
                         Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                         Tag="Enter first name"
                         Height="40"
                         Foreground="Black"
                         FontWeight="DemiBold"
                         PreviewKeyDown="FirstName_PreviewKeyDown"
                         PreviewTextInput="UppercaseOnly_PreviewTextInput"
                         DataObject.Pasting="UppercaseOnly_Pasting"
                         TextChanged="CustomerName_TextChanged"/>
                        <TextBlock x:Name="errCustomer1"
                           Text="* Fill the first name"
                           Foreground="#CC1F1F"
                           FontSize="12"
                           Visibility="Collapsed"
                           Margin="5,2,0,0"/>
                    </StackPanel>

                    <!-- Last Name -->
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,30,15,20">
                        <TextBlock Text="Last Name"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="#333"
                           Margin="0,0,0,8"/>
                        <TextBox x:Name="txtLastName"
                         Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                         Tag="Enter last name"
                         Height="40"
                         Foreground="Black"
                         FontWeight="DemiBold"
                         PreviewKeyDown="LastName_PreviewKeyDown"
                         PreviewTextInput="UppercaseOnly_PreviewTextInput"
                         DataObject.Pasting="UppercaseOnly_Pasting"
                         TextChanged="CustomerName_TextChanged"/>
                        <!-- No validation error for last name as per requirement -->
                    </StackPanel>

                    <!-- Phone Number -->
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="15,30,0,20" Background="Transparent">
                        <TextBlock Text="Phone Number" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                        <TextBox x:Name="txtPhone" 
                         Style="{StaticResource RoundedWatermarkTextBoxStyle}" 
                         Tag="+91 902 543 xxxx" 
                         Foreground="Black" 
                         FontWeight="DemiBold" 
                         Height="40"  
                         MaxLength="10" 
                         PreviewKeyDown="Phone_PreviewKeyDown" 
                         PreviewTextInput="NumbersOnly_PreviewTextInput" 
                         DataObject.Pasting="NumbersOnly_Pasting"/>
                        <TextBlock x:Name="errPhone1" Text="* Fill Phone Number" Foreground="#CC1F1F" FontSize="12" Visibility="Collapsed" Margin="5,2,0,0"/>
                    </StackPanel>

                    <!-- Hidden combined field -->
                    <TextBox x:Name="txtCustomer" Visibility="Collapsed"/>


                    <!-- Pricing Info (if any) could go in Row 3 -->

                    <!-- Proof Information -->
                    <Grid Grid.Row="4" Grid.ColumnSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,20">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- ROW 1 - COL 0 : ID TYPE -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,15,0">
                            <TextBlock Text="Proof Information" FontWeight="SemiBold" FontSize="14" Foreground="#222" Margin="0,0,0,14"/>
                            <TextBlock Text="ID Type" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>

                            <ComboBox x:Name="cmbIdType"
                             Height="40"
                             Padding="10,0"
                             FontSize="14"
                             Foreground="#333"
                             Background="White"
                             HorizontalContentAlignment="Left"
                             VerticalContentAlignment="Center"
                             MaxDropDownHeight="150"
                             PreviewKeyDown="IdType_PreviewKeyDown"
                             Style="{StaticResource WhiteComboBoxStyle}"
                             SelectionChanged="cmbIdType_SelectionChanged">
                                <ComboBoxItem x:Name="idPlaceholder" Content="Select ID" IsEnabled="False" IsSelected="True"/>
                                <ComboBoxItem Content="Aadhar" Style="{StaticResource WhiteComboBoxItemStyle}"/>
                                <ComboBoxItem Content="PNR Number" Style="{StaticResource WhiteComboBoxItemStyle}"/>
                                <ComboBoxItem Content="PAN ID" Style="{StaticResource WhiteComboBoxItemStyle}"/>
                            </ComboBox>

                            <TextBlock x:Name="errIdType1" Text="* Select ID Type" Foreground="#CC1F1F" FontSize="12" Visibility="Collapsed" Margin="5,2,0,0"/>
                        </StackPanel>

                        <!-- ROW 1 - COL 1 : ID INPUT -->
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="15,32,0,0">
                            <TextBlock x:Name="lblIdInput" Text="Enter ID Number" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>

                            <TextBox x:Name="txtIdNumber"
                             Foreground="Black" FontWeight="DemiBold"
                             Style="{StaticResource RoundedWatermarkTextBoxStyle}" 
                             Tag="ID Number" 
                             Height="40"
                             TextWrapping="NoWrap"
                             AcceptsReturn="False"
                             PreviewKeyDown="IdNumber_PreviewKeyDown"
                             PreviewTextInput="IdNumber_PreviewTextInput"
                             DataObject.Pasting="IdNumber_Pasting"/>

                            <TextBlock x:Name="errIdNumber1" Text="* Fill ID Number" Foreground="#CC1F1F" FontSize="12" Visibility="Collapsed" Margin="5,2,0,0"/>
                        </StackPanel>

                        <!-- ROW 2 - COL 0 : BOOKING DATE -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,15,15,0">
                            <TextBlock Text="Date" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtBookingDate"
                             Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                             Foreground="Black" FontWeight="DemiBold"
                             Height="40"
                             IsReadOnly="True"/>
                        </StackPanel>

                        <!-- ROW 2 - COL 1 : BOOKING TIME -->
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="15,15,0,0">
                            <TextBlock Text="Time" FontWeight="SemiBold" FontSize="14" Foreground="#333" Margin="0,0,0,8"/>
                            <TextBox x:Name="txtBookingTime"
                             Style="{StaticResource RoundedWatermarkTextBoxStyle}"
                             Foreground="Black" FontWeight="DemiBold"
                             Tag="HH:MM"
                             Height="40"
                             IsReadOnly="True"/>
                        </StackPanel>

                    </Grid>



                    <!-- Data grid for luggage items inserted inside the main grid -->
                    <Border Grid.Row="5" Grid.ColumnSpan="3" Margin="0,0,0,10" Background="#FFFFFF" CornerRadius="8" Padding="8">
                        <DataGrid x:Name="LuggageGrid"
                                  ItemsSource="{Binding Items}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserReorderColumns="False"
                                  CanUserResizeColumns="True"
                                  CanUserResizeRows="False"
                                  CanUserSortColumns="False"
                                  SelectionUnit="Cell"
                                  SelectionMode="Single"
                                  HeadersVisibility="Column"
                                  IsReadOnly="False"
                                  RowHeight="40"
                                  ColumnHeaderHeight="40"
                                  GridLinesVisibility="None"
                                  BorderThickness="0"
                                  Background="#FAFBFF"
                                  Margin="0"
                                  Grid.ColumnSpan="3"
                                  PreviewKeyDown="LuggageGrid_PreviewKeyDown"
                                  PreviewMouseLeftButtonDown="LuggageGrid_PreviewMouseLeftButtonDown"
                                  PreviewMouseWheel="LuggageGrid_PreviewMouseWheel"
                                  VerticalScrollBarVisibility="Hidden"
                                  HorizontalScrollBarVisibility="Hidden"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  MaxHeight="260"
                                  MinHeight="200"
                                  EnableRowVirtualization="True"
                                  EnableColumnVirtualization="False"
                                  ScrollViewer.CanContentScroll="True"
                                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                  ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                                  IsSynchronizedWithCurrentItem="False">

                            <DataGrid.Resources>
                                <!-- Cell Style -->
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                    <Setter Property="IsTabStop" Value="True"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        SnapsToDevicePixels="True">
                                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                      VerticalAlignment="Center"
                                                                      HorizontalAlignment="Stretch"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#E3F2FD"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                        <Trigger Property="IsEditing" Value="True">
                                            <Setter Property="Background" Value="White"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsSelected" Value="True"/>
                                                <Condition Property="IsEditing" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="White"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>

                                <!-- Row Style - Prevent row selection -->
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                    <Setter Property="IsTabStop" Value="False"/>
                                    <Setter Property="Focusable" Value="False"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F5F5F5"/>
                                        </Trigger>
                                        <!-- Prevent row selection visual state -->
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="White"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>

                                <!-- Column Header Style -->
                                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource LuggageDataGridHeaderStyle}"/>

                                <!-- ScrollBar Style - Completely hidden but functional -->
                                <Style TargetType="ScrollBar">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Width" Value="0"/>
                                    <Setter Property="Height" Value="0"/>
                                    <Setter Property="Opacity" Value="0"/>
                                    <Setter Property="IsTabStop" Value="False"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ScrollBar">
                                                <!-- Empty template to make scrollbar invisible -->
                                                <Border Background="Transparent" Width="0" Height="0"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>

                                <!-- S.No Column -->
                                <DataGridTemplateColumn Width="80">
                                    <DataGridTemplateColumn.Header>
                                        <TextBlock Text="S.No"
                                           Margin="10,0,0,0"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Left"/>
                                    </DataGridTemplateColumn.Header>

                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding SNo}"
                                               FontSize="14"
                                               FontWeight="DemiBold"
                                               Padding="10,8"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Background="Transparent"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>


                                <!-- Luggage Type -->
                                <DataGridTemplateColumn Header="Luggage Type" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="Transparent" Padding="8,4">
                                                <TextBlock Text="{Binding LuggageType}"
                                                   FontSize="14"
                                                   FontWeight="DemiBold"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center"
                                                   Background="Transparent"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>

                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox IsEditable="True"
                                              IsTextSearchEnabled="True"
                                              StaysOpenOnEdit="True"
                                              ItemsSource="{Binding DataContext.LuggageTypes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              Text="{Binding LuggageType, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                              Background="White"
                                              FontSize="14"
                                              FontWeight="DemiBold"
                                              Foreground="Black"
                                              BorderThickness="1"
                                              BorderBrush="#CCCCCC"
                                              Margin="2"
                                              Padding="8,4"
                                              MaxDropDownHeight="200"
                                              IsDropDownOpen="False"
                                              LostFocus="LuggageType_LostFocus"
                                              KeyUp="LuggageType_KeyUp"
                                              DropDownOpened="LuggageType_DropDownOpened"
                                              GotFocus="LuggageType_GotFocus"
                                              PreviewKeyDown="LuggageType_PreviewKeyDown"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>


                                <!-- Quantity -->
                                <DataGridTemplateColumn Header="Quantity" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="Transparent" Padding="8,4">
                                                <TextBlock Text="{Binding Quantity}"
                                                   FontSize="14"
                                                   FontWeight="DemiBold"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Background="Transparent"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>

                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <TextBox Text="{Binding Quantity, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                             PreviewTextInput="NumbersOnly_PreviewTextInput"
                                             Background="White"
                                             Foreground="Black"
                                             FontSize="14"
                                             FontWeight="DemiBold"
                                             BorderThickness="1"
                                             BorderBrush="#CCCCCC"
                                             Margin="2"
                                             Padding="8,4"
                                             TextAlignment="Center"
                                             VerticalContentAlignment="Center"
                                             MaxLength="2"
                                             LostFocus="Quantity_LostFocus"
                                             PreviewKeyDown="Quantity_PreviewKeyDown"
                                             GotFocus="Quantity_GotFocus"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>


                                <!-- Price -->
                                <DataGridTextColumn Header="Price (₹)"
                                        Binding="{Binding Price, StringFormat=0.00}"
                                        IsReadOnly="True"
                                        Width="100">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                            <Setter Property="Padding" Value="8,4"/>
                                            <Setter Property="FontSize" Value="14"/>
                                            <Setter Property="FontWeight" Value="DemiBold"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Focusable" Value="False"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>



                                <!-- Total -->
                                <DataGridTextColumn Header="Total (₹)"
                                    Binding="{Binding TotalAmount, StringFormat=0.00}"
                                    IsReadOnly="True"
                                    Width="120">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                            <Setter Property="Padding" Value="8,4"/>
                                            <Setter Property="FontSize" Value="14"/>
                                            <Setter Property="FontWeight" Value="DemiBold"/>
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Focusable" Value="False"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>


                            </DataGrid.Columns>

                        </DataGrid>
                    </Border>

                    <!-- Submit / Cancel Buttons -->
                    <Grid Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="0,20,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                            <Button x:Name="GenerateBillButton" Content="Generate Bill" Width="249" Height="40" Background="#232323" Foreground="white" FontSize="16" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0" Click="GenerateBill_Click"/>
                            <Button Content="Cancel" Width="243" Height="40" Background="#BDBCBC" Foreground="White" FontSize="16" FontWeight="SemiBold" BorderBrush="#999" BorderThickness="0" Cursor="Hand"/>
                        </StackPanel>
                    </Grid>
                </Grid>


            </Grid>
        </Border>


    </Grid>
</UserControl>
